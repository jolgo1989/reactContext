// Importa la función createContext desde React para crear un contexto global
import { createContext } from "react";

// Crea un nuevo contexto llamado "ConunterContext"
// Este contexto será usado para compartir datos relacionados con un contador entre componentes
export const ConunterContext = createContext();
